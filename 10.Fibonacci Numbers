using System;

class Program
{
    static void Main()
    {
        int n = int.Parse(Console.ReadLine());
        int x1 = 0;
        int x2 = 1;
        int x3=1;
        if (n<0)
        {
            Console.WriteLine("Number is out of range!");
            return;
        }
        if (n==1)
        {
            Console.WriteLine(x1);
        }
        else
        {
            Console.Write(x1+" "+x2+" ");
            for (int i = 2; i < n; i++)
            {
                x3=x2+x1;
                Console.Write(x3+" ");
                x1 = x2;
                x2=x3;
            }
        }
    }
}

